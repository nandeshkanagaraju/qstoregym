# Virtual Environments
venv/
.venv/
env/

# Python Cache
__pycache__/
*.py[cod]
*$py.class

# Environment Variables (IMPORTANT: Never push API keys)
.env
.env.bak
.env.*

# Trained RL Models (Optional - typically ignored to save space, but you can remove this if you want to push them)
# Trained RL Models - Uploading required for OpenEnv!
# *.zip removed so we can upload our models
# OS Files
.DS_Store
checkpoints/
*.zip
